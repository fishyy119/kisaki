# create-kisaki-extension

Scaffold a minimal single-entry Kisaki extension project.

```bash
npm create kisaki-extension my-extension
```

Generated projects use `manifest.json`, `src/index.ts`, `tsdown.config.ts`, and the `kisx` CLI.
