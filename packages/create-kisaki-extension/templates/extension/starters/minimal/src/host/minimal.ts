import type { ExtensionContext } from '@kisaki3/extension-sdk'

/** Activates the minimal starter without registering contributions. */
export function activateStarter(context: ExtensionContext): void {
  void context
}
