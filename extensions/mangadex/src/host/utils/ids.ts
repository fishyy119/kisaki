export const MANGADEX_STORAGE_KEYS = {
  settings: 'settings.v1',
  syncState: 'sync-state.v1'
} as const

export const MANGADEX_SECRET_KEYS = {
  credentials: 'auth.credentials',
  token: 'auth.token'
} as const
