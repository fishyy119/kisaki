export const MAL_STORAGE_KEYS = {
  settings: 'settings.v1',
  syncState: 'sync-state.v1'
} as const

export const MAL_SECRET_KEYS = {
  token: 'auth.token',
  pendingLogin: 'auth.pending-login'
} as const
