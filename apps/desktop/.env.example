# Base URL the in-app changelog reader fetches release notes from.
# Scraper credentials are not build-time values: every source is a built-in
# extension, and its credentials are entered in that extension's settings.
VITE_KISAKI_CHANGELOG_BASE_URL=
